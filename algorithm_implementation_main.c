#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "function.h"

int main(){
    struct node *map=map_generator(1000,100,5,100,100);
    printf("Value = %d\nx = %d\ny = %d", 100, 100,100);
    return 0;
}